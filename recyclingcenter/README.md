# Setup a recycling center domain
* There are multiple container for drop-off,
* Every container has a fixed price per kilo,
* Every time a drop-off has taken place, a return to the load bridge is needed.